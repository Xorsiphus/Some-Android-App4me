import React from 'react';
import { LogBox, StyleSheet, SafeAreaView, FlatList, View, Text, ScrollView, Button, TouchableOpacity, TouchableWithoutFeedback } from 'react-native'

function RecipesScreen(props) {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    );
}

export default RecipesScreen;